name = "tool_builder"
description = "Creates AI SDK v6 tool definitions from natural language specifications."
model = "gpt-5.4"
model_reasoning_effort = "high"
sandbox_mode = "read-only"
developer_instructions = """
Create Vercel AI SDK v6 tool definitions. Always use inputSchema with zod (not parameters).
Tools should query the Turso database via Drizzle ORM. Return structured JSON that the UI can render.
Follow the pattern: tool({ description, inputSchema: z.object({...}), execute: async ({...}) => {...} })
"""
